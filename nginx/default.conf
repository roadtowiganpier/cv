server {
    listen 80;
    server_name _;

    # Landing page at root
    location / {
        root /var/www/landing;
        index index.html;
        try_files $uri $uri/ =404;
    }

    # Ghost blog at /blog
    location /blog {
        proxy_pass http://ghost:2368;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ACME challenge for Let's Encrypt (used when adding SSL)
    location /.well-known/acme-challenge/ {
        root /var/www/landing;
    }
}

# HTTPS server block - uncomment when SSL certificate is ready
# server {
#     listen 443 ssl;
#     server_name christianbakerblog.candyfairstudio.com;
#
#     ssl_certificate /etc/letsencrypt/live/christianbakerblog.candyfairstudio.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/christianbakerblog.candyfairstudio.com/privkey.pem;
#
#     location / {
#         root /var/www/landing;
#         index index.html;
#         try_files $uri $uri/ =404;
#     }
#
#     location /blog {
#         proxy_pass http://ghost:2368;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
